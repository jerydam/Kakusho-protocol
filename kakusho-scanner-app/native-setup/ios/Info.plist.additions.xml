<!--
  Merge these keys into the <dict> of ios/KakushoScanner/Info.plist
  (the file generated by `npx react-native init`).

  NFCReaderUsageDescription is required by Apple for any NFC tag reading —
  the app will crash on launch attempting to start an NFC session without it.

  com.apple.developer.nfc.readersession.iso7816.select-identifiers restricts
  which application identifiers (AIDs) trigger your reader session.
  A0000002471001 = the ICAO MRTD application AID (ePassports/eMRTDs).
  00000000000000 is included by several reference implementations as a
  wildcard fallback for non-standard chips — confirm whether you need it
  for the document types you actually support.
-->
<key>NFCReaderUsageDescription</key>
<string>Kakushō needs NFC access to read your passport chip for identity verification.</string>

<key>com.apple.developer.nfc.readersession.iso7816.select-identifiers</key>
<array>
    <string>A0000002471001</string>
    <string>00000000000000</string>
</array>
